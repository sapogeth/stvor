# ==============================================================================
# Ilyazh Web App - Environment Template
# ==============================================================================
# COPY THIS TO .env.local AND FILL WITH REAL VALUES (DO NOT COMMIT .env.local)

# ==============================================================================
# PUBLIC VARIABLES (exposed to browser)
# ==============================================================================

# Relay Server URL (replace after deploying relay)
# Local dev: http://localhost:3001
# Production: https://your-relay-url.up.railway.app
NEXT_PUBLIC_RELAY_URL=http://localhost:3001

# Clerk Authentication (get from https://dashboard.clerk.com)
# Get your keys from: https://dashboard.clerk.com/apps/YOUR_APP/api-keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk___FILL_ME__

# Supabase (OPTIONAL - only if using Supabase client-side features)
# These are PUBLIC keys - safe to expose in browser
NEXT_PUBLIC_SUPABASE_URL=https://uppanfnstjeybiseolmo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVwcGFuZm5zdGpleWJpc2VvbG1vIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIwOTQ5NjgsImV4cCI6MjA3NzY3MDk2OH0.Zx_GTBxZ0NnbWxF19KV8hDBrSpR6G5ZHPW5md3pa9jU

# ==============================================================================
# SERVER-SIDE SECRETS (set in Vercel dashboard, DO NOT COMMIT)
# ==============================================================================

# Clerk Secret Key (NEVER expose to browser)
CLERK_SECRET_KEY=sk___FILL_ME__

# ==============================================================================
# Production-Safe Logging Configuration
# ==============================================================================
# SECURITY: All debug flags default to 0 (disabled) in production
# Enable ONLY in local development when debugging specific issues
# WARNING: Never enable in production - may leak sensitive data

# Crypto debugging (handshake, ratchet, message encryption/decryption)
# Shows redacted previews of plaintext (first 32 chars) and session IDs
NEXT_PUBLIC_DEBUG_CRYPTO=0

# Auth debugging (JWT tokens, identity management)
# Shows redacted JWT token prefixes (first 20 chars)
NEXT_PUBLIC_DEBUG_AUTH=0

# Sync debugging (relay communication, message polling)
NEXT_PUBLIC_DEBUG_SYNC=0

# Identity/keystore debugging (IndexedDB operations)
NEXT_PUBLIC_DEBUG_IDENTITY=0

# ==============================================================================
# Development Configuration
# ==============================================================================
# Next.js development settings
# Uncomment for local development
# NEXT_PUBLIC_DEV_MODE=true

# ==============================================================================
# Security Notes
# ==============================================================================
# 1. All encryption happens client-side - relay server only stores ciphertext
# 2. Private keys are stored encrypted in IndexedDB with password-based encryption
# 3. Always use HTTPS in production (both web app and relay server)
# 4. The relay URL should use wss:// protocol in production
# 5. Content Security Policy is configured in next.config.ts
# 6. Never expose private keys or passwords in environment variables
# 7. Consider using a reverse proxy (nginx, Cloudflare) for production

# ==============================================================================
# Production Example
# ==============================================================================
# NEXT_PUBLIC_RELAY_URL=https://relay.example.com
